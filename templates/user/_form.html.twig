{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('Css/home.css') }}">
{% endblock %}

{{ form_start(form, {'attr': {'id': 'profile-form', 'data-action-url': path('user_edit', {'id': user.id})}}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}

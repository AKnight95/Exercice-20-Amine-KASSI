{% extends 'base.html.twig' %}

{% block title %}Animaux{% endblock %}

{% block body %}
    <h1>Animaux</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ animaux.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ animaux.name }}</td>
            </tr>
            <tr>
                <th>Identifiant</th>
                <td>{{ animaux.identifiant }}</td>
            </tr>
            <tr>
                <th>DateDeNaissance</th>
                <td>{{ animaux.dateDeNaissance ? animaux.dateDeNaissance|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Espece</th>
                <td>{{ animaux.espece }}</td>
            </tr>
            <tr>
                <th>Variete</th>
                <td>{{ animaux.variete }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_animaux_index') }}">back to list</a>

    <a href="{{ path('app_animaux_edit', {'id': animaux.id}) }}">edit</a>

    {{ include('animaux/_delete_form.html.twig') }}
{% endblock %}
